name: print_widget
description: "Capture static Flutter widgets as PNG images for LLM visual verification."
version: 0.1.0

environment:
  sdk: ^3.11.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  args: ^2.4.0
  path: ^1.9.0
  yaml: ^3.1.0

dev_dependencies:
  flutter_lints: ^6.0.0

executables:
  print_widget: print_widget
