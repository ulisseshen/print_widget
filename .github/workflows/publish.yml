name: Publish to pub.dev

on:
  push:
    branches: [main]
    paths:
      - "lib/**"
      - "pubspec.yaml"

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: dart-lang/setup-dart@v1
        with:
          sdk: "3.11.0"

      - name: Check if version changed
        id: version-check
        run: |
          CURRENT_VERSION=$(grep '^version:' pubspec.yaml | sed 's/version: //')
          echo "version=$CURRENT_VERSION" >> $GITHUB_OUTPUT

          # Check if this version already exists on pub.dev
          HTTP_STATUS=$(curl -s -o /dev/null -w "%{http_code}" "https://pub.dev/api/packages/print_widget_flutter/versions/$CURRENT_VERSION")
          if [ "$HTTP_STATUS" = "200" ]; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "::notice::Version $CURRENT_VERSION already published — skipping"
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "::notice::Version $CURRENT_VERSION is new — will publish"
          fi

      - name: Install dependencies
        if: steps.version-check.outputs.exists == 'false'
        run: dart pub get

      - name: Analyze
        if: steps.version-check.outputs.exists == 'false'
        run: dart analyze lib/

      - name: Dry run
        if: steps.version-check.outputs.exists == 'false'
        run: dart pub publish --dry-run

      - name: Setup pub credentials
        if: steps.version-check.outputs.exists == 'false'
        run: |
          mkdir -p $HOME/.config/dart
          echo '${{ secrets.PUB_CREDENTIALS }}' > $HOME/.config/dart/pub-credentials.json

      - name: Publish to pub.dev
        if: steps.version-check.outputs.exists == 'false'
        run: dart pub publish --force
