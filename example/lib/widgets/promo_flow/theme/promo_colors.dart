import 'package:flutter/material.dart';

class PromoColors {
  // Primary brand
  static const primary = Color(0xFF0BA284);
  static const primaryLight = Color(0xFFE6F7F2);
  static const primaryForeground = Colors.white;

  // Surfaces
  static const surface = Colors.white;
  static const surfaceSecondary = Color(0xFFF7F8FA);
  static const surfaceMuted = Color(0xFFF1F3F5);

  // Text
  static const textPrimary = Color(0xFF1A1D21);
  static const textSecondary = Color(0xFF6B7280);
  static const textMuted = Color(0xFF9CA3AF);

  // Semantic
  static const success = Color(0xFF10B981);
  static const successLight = Color(0xFFECFDF5);
  static const danger = Color(0xFFEF4444);
  static const dangerLight = Color(0xFFFEF2F2);
  static const warning = Color(0xFFF59E0B);
  static const warningLight = Color(0xFFFFFBEB);
  static const info = Color(0xFF3B82F6);
  static const infoLight = Color(0xFFEFF6FF);

  // Gauge colors
  static const gaugeGreen = Color(0xFF10B981);
  static const gaugeOrange = Color(0xFFF59E0B);
  static const gaugeRed = Color(0xFFEF4444);
  static const gaugeTrack = Color(0xFFE5E7EB);

  // Borders
  static const border = Color(0xFFE5E7EB);
  static const borderLight = Color(0xFFF3F4F6);

  // Brown (for store/loja icons)
  static const brown = Color(0xFF92400E);
  static const brownLight = Color(0xFFFEF3C7);
}
